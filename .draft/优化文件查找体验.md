# 优化文件查找体验

## 1. 背景

`aily-blockly` 当前在 `aily-chat/aily-lex` 体系里已经提供了 `glob_search` 和 `grep_search` 两类工具，但实际体验仍然不稳定，尤其是在以下场景里比较明显：

- 用户只记得文件名的一部分，或者只记得一个模糊概念，而不是完整 glob。
- 模型需要先快速定位候选文件，再进入 `read_file` / `edit_file`。
- 仓库里存在空文件、配置文件、隐藏文件、非代码文本、目录级入口文件时，查找结果不完整。
- 模型需要自己判断该先用 `glob_search` 还是 `grep_search`，会产生额外推理成本。

这份报告的目标不是新增一套新的文件搜索体系，而是在保留 `glob_search` / `grep_search` 这套交互模型的前提下，优化它们的底层实现和提示词引导，让模型在更多场景下能更快打到高命中结果。

## 2. 现在的文件查找流程，以及文件查找的目标

### 2.1 文件查找的目标

当前文件查找服务的真实目标，是为模型和用户提供一个低成本的“候选文件定位”能力，支撑后续动作：

- 快速找到应该读取的文件。
- 在不打开大量文件的前提下收窄搜索范围。
- 在大型仓库里尽量快地返回少量高价值候选结果。
- 为后续 `read_file`、`edit_file`、`create_file`、`delete_file` 等工具提供入口。

换句话说，文件查找不是终点，而是整个代码理解和修改流程里的第一跳。

### 2.2 当前流程总览

当前链路不是直接由 `aily-lex` 自己决定，而是宿主在中间插入了一层 Blockly/Electron 搜索扩展。

主链路如下：

1. 模型调用 `glob_search` 或 `grep_search`。
2. `aily-lex` 的通用搜索层先尝试 `host.getExtension('search')`。
3. `aily-blockly` 在宿主侧注入 `searchFiles` / `searchText` 实现。
4. 如果宿主实现可用，`aily-lex` 就不会再走它自己的 `rg --files` / `rg grep` 路径。
5. 最终结果返回给模型，模型再决定是否继续 `read_file`。

### 2.3 当前关键代码位置

- `aily-lex` 工具入口：
  - [../aily-lex/src/tools/glob/globTool.ts](/Users/xiewendao/Documents/aily/aily-lex/src/tools/glob/globTool.ts:32)
  - [../aily-lex/src/tools/grep/grepTool.ts](/Users/xiewendao/Documents/aily/aily-lex/src/tools/grep/grepTool.ts:49)
- `aily-lex` 搜索总入口：
  - [../aily-lex/src/tools/common/search.ts](/Users/xiewendao/Documents/aily/aily-lex/src/tools/common/search.ts:111)
- Blockly 宿主注入的搜索扩展：
  - [src/app/tools/aily-chat/helpers/lex-agent-bootstrap.ts](/Users/xiewendao/Documents/aily/aily-blockly/src/app/tools/aily-chat/helpers/lex-agent-bootstrap.ts:1835)
- Electron 侧 ripgrep IPC 实现：
  - [electron/ripgrep.js](/Users/xiewendao/Documents/aily/aily-blockly/electron/ripgrep.js:230)
- 工具说明定义：
  - [src/app/tools/aily-chat/tools/discovery-tool-definitions.ts](/Users/xiewendao/Documents/aily/aily-blockly/src/app/tools/aily-chat/tools/discovery-tool-definitions.ts:287)

### 2.4 `glob_search` 当前实际流程

当前 `glob_search` 在 Blockly 宿主里的实际执行流程如下：

1. `glob_search` 调用 `searchFiles()`。
2. `searchFiles()` 优先走宿主扩展 `host.getExtension('search').searchFiles(...)`。
3. Blockly 宿主扩展里，`searchFiles()` 先调用 `electronAPI.ripgrep.listAllContentFiles(...)`。
4. `listAllContentFiles()` 的底层命令是 `rg -l --max-count=1 .`。
5. 它先枚举“包含可搜索内容的文件”，再在 JavaScript 里用 `globToRegex()` 过滤路径。
6. 如果结果不够，再 fallback 到宿主文件系统递归遍历，并套用一组手写忽略目录规则。

对应代码：

- 宿主覆盖入口：
  - [src/app/tools/aily-chat/helpers/lex-agent-bootstrap.ts](/Users/xiewendao/Documents/aily/aily-blockly/src/app/tools/aily-chat/helpers/lex-agent-bootstrap.ts:1858)
- 内容文件枚举：
  - [electron/ripgrep.js](/Users/xiewendao/Documents/aily/aily-blockly/electron/ripgrep.js:230)
- fallback 文件系统遍历：
  - [src/app/tools/aily-chat/helpers/lex-agent-bootstrap.ts](/Users/xiewendao/Documents/aily/aily-blockly/src/app/tools/aily-chat/helpers/lex-agent-bootstrap.ts:1956)

### 2.5 `grep_search` 当前实际流程

`grep_search` 当前流程相对直接：

1. `grep_search` 调用 `searchText()`。
2. `searchText()` 同样优先走宿主扩展 `host.getExtension('search').searchText(...)`。
3. Blockly 宿主扩展里会调用 `electronAPI.ripgrep.searchContent(...)`。
4. Electron 侧用 ripgrep 做内容搜索，再在 JavaScript 层进行结果解析、截断、部分二次展开。

对应代码：

- `aily-lex` 入口：
  - [../aily-lex/src/tools/common/search.ts](/Users/xiewendao/Documents/aily/aily-lex/src/tools/common/search.ts:289)
- 宿主覆盖实现：
  - [src/app/tools/aily-chat/helpers/lex-agent-bootstrap.ts](/Users/xiewendao/Documents/aily/aily-blockly/src/app/tools/aily-chat/helpers/lex-agent-bootstrap.ts:1896)
- Electron 侧内容搜索：
  - [electron/ripgrep.js](/Users/xiewendao/Documents/aily/aily-blockly/electron/ripgrep.js:264)

## 3. 现在的文件查找流程有什么问题

### 3.1 `glob_search` 实际上不是“文件搜索”，而是“内容文件枚举 + JS 过滤”

这是当前最大的问题。

`listAllContentFiles()` 不是从文件系统枚举文件，而是执行：

```sh
rg -l --max-count=1 .
```

这个策略会天然偏向“非空、可被内容搜索命中的文本文件”，而不是“工作区中的所有候选文件”。因此以下对象可能天然缺失或表现不稳定：

- 空文件
- 部分隐藏文件
- 某些配置文件
- 部分非文本文件
- 目录本身
- 没有内容但路径很重要的入口文件

这会导致用户以“文件名思维”搜索时，结果和直觉不一致。

### 3.2 `glob_search` 的命中率依赖扫描顺序，结果稳定性不足

当前 `glob_search` 结果本质上是：

- 先拿到一批文件
- 再用 glob 正则过滤
- 截断返回前 N 个

这意味着结果质量很大程度取决于“扫描顺序 + 截断”，而不是“路径是否真正匹配得更好”。对于以下查询，命中率和稳定性都会受影响：

- `pin map`
- `search session`
- `config`
- `builder`
- 只记得文件名一部分或路径片段

在当前方案下，即使 glob 写得没问题，也可能因为候选集来源不对或截断过早，导致真正目标文件没有稳定出现在前面。

### 3.3 ignore 规则不统一

当前路径上至少有两套 ignore 语义：

- ripgrep 自己的 ignore 规则
- fallback 文件系统遍历里的手写忽略目录集合

手写忽略规则见：

- [src/app/tools/aily-chat/helpers/lex-agent-bootstrap.ts](/Users/xiewendao/Documents/aily/aily-blockly/src/app/tools/aily-chat/helpers/lex-agent-bootstrap.ts:2025)
- [../aily-lex/src/tools/common/search.ts](/Users/xiewendao/Documents/aily/aily-lex/src/tools/common/search.ts:102)

问题在于：

- 它们不能完整表达项目自己的 `.gitignore` / `.ignore` 语义。
- 前后两段逻辑的结果可能不一致。
- 用户很难预测“为什么这个文件有时能搜到，有时搜不到”。

### 3.4 工具语义和实际行为不完全一致

工具描述里写了：

- `glob_tool` “Returns matching file paths sorted by modification time”
- `grep_tool` 兼具文件模式和内容模式

但从当前宿主实现看：

- 没有按修改时间排序。
- `glob_search` 的核心实现不是“按文件名查找所有文件”，而是“内容文件候选集过滤”。
- `grep_search` 侧还存在宿主实现与 `aily-lex` 输入能力不完全对齐的问题，例如 `includeIgnoredFiles` 并没有在 Blockly 宿主搜索扩展里完整体现。

这会导致工具文档、模型预期和真实行为之间出现偏差。

### 3.5 模型需要自己决定“先 glob 还是先 grep”，认知成本偏高

目前 agent 提示词倾向于这样分工：

- 只知道文件名模式时用 `glob_search`
- 要按内容搜时用 `grep_search`

这套思路在“工具使用者是工程师”时成立，但对模型来说并不是最优交互形态。原因是：

- 用户的问题往往是模糊的，不是标准 glob 查询。
- 模型要先推理“这是文件名问题还是内容问题”。
- 推理错误会直接导致首轮搜索质量下降。

本质上，这是把“搜索策略”的责任推给了模型，而不是由系统自己提供更强的默认搜索器。

### 3.6 提示词对高命中搜索的引导还不够具体

当前提示词虽然区分了 `glob_search` 和 `grep_search` 的用途，但对“如何提高首轮命中率”引导还不够细。

缺少的引导主要包括：

- 当用户只给概念词时，应先把概念拆成更适合路径匹配的 glob 片段。
- 什么时候应该先 `glob_search` 收窄文件集，再 `grep_search` 搜内容。
- `grep_search` 的 `includePattern` 应该如何配合 `glob_search` 结果一起使用。
- 对常见配置文件、文档文件、测试文件，应该优先尝试哪些 glob 模式。
- 当首轮查询无结果时，应该如何逐步放宽 pattern，而不是直接切换到大范围 grep。

## 4. 优化方向

这份报告聚焦一个更轻、更直接的改造方向：

- 不引入新的 `file_search` 工具
- 不引入复杂的索引或 session
- 不改变模型看到的工具集合
- 只优化 `glob_search` / `grep_search` 的底层实现和提示词引导

核心原则有两条：

> 1. 用 Node.js 自带的 `glob` 能力替换 `listAllContentFiles()`，让 `glob_search` 真正按路径模式匹配文件，而不是先枚举“有内容的文件”再过滤。
>
> 2. 强化 `glob_search` / `grep_search` 的提示词，引导模型先缩小文件范围，再做内容搜索，从而提升首轮命中率。

### 4.1 目标流程

建议把当前 `glob_search` 的宿主实现收敛成下面这条链路：

1. 模型调用 `glob_search`
2. `aily-lex` 仍然走宿主扩展 `host.getExtension('search').searchFiles(...)`
3. 宿主 `searchFiles()` 直接调用 Node.js 自带 `glob`
4. `glob` 返回真实文件系统里的路径候选
5. 在宿主侧统一做：
   - 相对路径转换
   - `limit` 截断
   - 路径规范化
   - 可选排序
6. 结果直接返回给 `glob_search`

和当前方案相比，最大的变化是：

- 不再通过 `rg -l .` 推导文件列表
- 不再依赖“文件必须有内容”这一前提
- 不再先做“内容文件枚举”，再做“路径过滤”

### 4.2 为什么用 Node.js 自带 `glob` 更合适

这个优化方向的好处很明确：

1. 它和 `glob_search` 的工具语义一致。
   - 用户传入的是 glob pattern
   - 宿主执行的也是真正的 glob 匹配

2. 它比现在的 `listAllContentFiles()` 更完整。
   - 空文件可以匹配
   - 纯配置文件可以匹配
   - 不依赖文件内容是否可搜索

3. 它比“手写 `globToRegex + 遍历结果过滤`”更可靠。
   - 不需要自己维护 glob 语法兼容性
   - 减少边界错误

4. 它比引入新架构更轻。
   - 不需要新增搜索协议
   - 不需要大改 agent 工具体系
   - 不需要引入复杂索引层

### 4.3 `grep_search` 也需要一起优化，但重点是“收窄范围”

这次不建议重写 `grep_search`，但建议明确它的职责：

- `glob_search` 负责找可能相关的文件路径
- `grep_search` 负责在收窄后的文件集合里找具体内容

因此 `grep_search` 的优化重点不是换引擎，而是提升“配合 `includePattern` 后的命中率”：

1. 优先让模型先做一轮 `glob_search`
2. 从 `glob_search` 结果里总结出更小的 `includePattern`
3. 再执行 `grep_search`

例如：

- 先用 `**/*pin*map*`
- 再用 `grep_search(query="I2C", includePattern="**/*pin*map*")`

这样比直接在整个仓库里 grep 更容易高命中，也更省结果预算。

### 4.4 需要替换的核心点

当前最应该被替换的是这里：

- [src/app/tools/aily-chat/helpers/lex-agent-bootstrap.ts](/Users/xiewendao/Documents/aily/aily-blockly/src/app/tools/aily-chat/helpers/lex-agent-bootstrap.ts:1858)
- [electron/ripgrep.js](/Users/xiewendao/Documents/aily/aily-blockly/electron/ripgrep.js:230)

当前逻辑是：

1. `searchFiles()` 调 `ripgrep.listAllContentFiles()`
2. `listAllContentFiles()` 用 `rg -l --max-count=1 .`
3. JS 侧再用 `globToRegex()` 过滤

目标逻辑是：

1. `searchFiles()` 直接调用主进程提供的 `glob` IPC
2. 主进程或 preload 用 Node.js 自带 `glob` 按 pattern 返回文件
3. 宿主只负责轻量整理结果

### 4.5 建议的实现方式

建议把实现放在 Electron 主进程或 Node 侧，而不是 renderer 里自己递归遍历。

推荐做法：

1. 在 `electron/main.js` 增加新的 IPC：
   - 例如 `glob-search-files`

2. 在 `electron/preload.js` 增加对应桥接：
   - 例如 `electronAPI.workspaceSearch.globFiles(...)`

3. 在宿主扩展 `createBlocklySearchExtension()` 里，把 `searchFiles()` 改成调用新的 `globFiles()`

4. 保留 `searchText()` 继续走 ripgrep

这样职责会更清楚：

- `glob_search`：Node.js `glob`
- `grep_search`：ripgrep

### 4.6 建议的输入输出约束

`glob_search` 当前对外输入已经够用，不建议新增太多字段。

建议继续保留：

- `pattern`
- `cwd`
- `maxResults`

宿主实现里建议统一做这些规则：

1. 返回相对路径，而不是绝对路径
2. 路径统一为 `/`
3. 严格执行 `maxResults`
4. 默认排除常见无关目录
5. 行为尽量稳定，不依赖扫描顺序的偶然性

### 4.7 ignore 规则建议

这里不建议一开始就做得很重，但至少要统一。

建议优先采用以下策略：

1. 默认排除：
   - `.git`
   - `node_modules`
   - `.cache`
   - `.aily`
   - `.aily_checkpoints`
   - `__pycache__`

2. 是否跟随 `.gitignore`
   - 如果 Node.js 当前 `glob` 方案能方便接入，则优先尊重
   - 如果当前运行时接入成本较高，可以先保持“显式排除目录”为主

3. 无论如何，都不要再混用：
   - “ripgrep 的内容文件枚举语义”
   - “手写目录遍历语义”

至少要保证 `glob_search` 只有一套稳定语义。

### 4.8 排序建议

不需要把排序做复杂，但建议至少不要继续依赖枚举顺序。

一个足够轻的排序策略是：

1. 先按路径深度升序
2. 再按文件名长度升序
3. 最后按路径字典序升序

这样通常能让：

- 更短的入口文件
- 更浅层的配置文件
- 更直观的目标文件

排在前面，结果会比“谁先扫描到谁在前面”更稳定。

### 4.9 fallback 建议

当前实现里 `searchFiles()` 在宿主扩展路径下还会 fallback 到文件系统遍历。

优化后建议：

1. 首选 Node.js `glob`
2. 如果 `glob` 失败，再 fallback 到现有宿主文件系统递归
3. fallback 时仍然只做“真实文件系统路径匹配”
4. 不再 fallback 到 `listAllContentFiles()`

这样即使回退，语义也还是“找文件”，而不是“找有内容的文件”。

### 4.10 提示词引导建议

为了提高首轮命中率，建议同步修改 `glob_search` / `grep_search` 的提示词和工具描述，重点不是增加术语，而是增加可执行的搜索策略。

建议补充以下规则：

1. 当用户只给概念词时，优先把词拆成路径片段，再构造 glob。
   - 例如 `tool invocation formatter` 可转成 `**/*tool*invocation*formatter*`

2. 当用户要找“某类文件”时，优先加上扩展名或目录范围。
   - 例如 `**/*.md`
   - 例如 `src/**/*.ts`
   - 例如 `docs/**/pin*`

3. 当首轮 `glob_search` 无结果时，优先逐步放宽 pattern。
   - 从 `src/**/file-search*`
   - 放宽到 `**/*file-search*`
   - 再放宽到 `**/*search*`

4. `grep_search` 默认不应该直接扫全仓。
   - 先 `glob_search`
   - 再把 glob 模式或其结果映射成 `includePattern`

5. 当目标可能是配置文件、说明文档、入口文件时，优先尝试：
   - `**/*config*`
   - `**/*readme*`
   - `**/*index*`
   - `**/*main*`

建议修改位置：

- [../aily-lex/src/tools/glob/prompt.ts](/Users/xiewendao/Documents/aily/aily-lex/src/tools/glob/prompt.ts:10)
- [../aily-lex/src/tools/grep/prompt.ts](/Users/xiewendao/Documents/aily/aily-lex/src/tools/grep/prompt.ts:10)
- [src/app/tools/aily-chat/tools/discovery-tool-definitions.ts](/Users/xiewendao/Documents/aily/aily-blockly/src/app/tools/aily-chat/tools/discovery-tool-definitions.ts:287)

## 5. 这样优化后会带来什么体验提升

### 5.1 `glob_search` 会真正符合直觉

优化前，用户以为自己在“按路径查找文件”，实际系统在“按内容文件候选集查找路径”。

优化后，用户传什么 glob，系统就按 glob 匹配真实文件路径。这会让结果更符合用户预期，也更符合工具名称。

### 5.2 空文件和配置文件不再天然丢失

这是最直接的体验提升。

例如这些文件在优化后都应该能稳定搜索到：

- `.env.local`
- `tsconfig.json`
- `README.md`
- `config/*.json`
- 空白占位文件

### 5.3 结果稳定性更高

优化前，结果是否出现，部分取决于：

- 文件有没有内容
- ripgrep 是否把它识别为内容文件
- fallback 有没有扫到它

优化后，结果只取决于：

- 路径是否匹配 glob
- ignore 规则是否排除它

这会让行为更可解释。

### 5.4 首轮命中率会更高

这次不引入新的工具，但只要做到两件事：

- `glob_search` 真正按路径查找
- 提示词明确引导“先 glob 收窄，再 grep 命中”

模型的首轮搜索命中率就会明显提高。

这比单纯增加工具数量更实际，因为问题的关键不是“缺工具”，而是“现有工具没有被正确实现和正确引导”。

### 5.5 维护成本更低

把路径匹配交给 Node.js 自带 `glob`，会减少以下维护负担：

- 自己兼容 glob 语法
- 自己拼装不可靠的“内容文件候选集”
- 自己修很多路径边界问题

## 6. 实现建议

### 6.1 最小改造方案

最小改造可以只改 `glob_search` 这一条链路：

1. 新增一个基于 Node.js 自带 `glob` 的 IPC 能力
2. 修改 Blockly 宿主扩展的 `searchFiles()`
3. 删除 `listAllContentFiles()` 在 `glob_search` 路径上的参与
4. 保持 `grep_search` 的底层搜索引擎不动
5. 同步补充 `glob_search` / `grep_search` 的提示词引导

这条路线风险最小，收益也最直接。

### 6.2 建议保留的现有部分

以下部分建议先保留，不需要这次一起重构：

- `grep_search`
- `searchText()`
- Electron 侧 ripgrep 内容搜索
- `aily-lex` 工具接口
- `glob_search` / `grep_search` 的调用方式

这次只需要把“文件搜索的文件来源”改正确。

### 6.3 建议修改的文件

重点关注：

- [src/app/tools/aily-chat/helpers/lex-agent-bootstrap.ts](/Users/xiewendao/Documents/aily/aily-blockly/src/app/tools/aily-chat/helpers/lex-agent-bootstrap.ts:1858)
- [electron/preload.js](/Users/xiewendao/Documents/aily/aily-blockly/electron/preload.js:668)
- [electron/main.js](/Users/xiewendao/Documents/aily/aily-blockly/electron/main.js:3013)
- [electron/ripgrep.js](/Users/xiewendao/Documents/aily/aily-blockly/electron/ripgrep.js:230)

建议调整方向：

- 在 `main.js` 增加新的 glob IPC
- 在 `preload.js` 暴露新的 bridge
- 在 `lex-agent-bootstrap.ts` 的 `searchFiles()` 中调用新的 glob bridge
- `ripgrep.js` 保留给内容搜索，不再承担文件枚举职责

## 7. 如何验证

验证目标不是只验证“命令能跑通”，而是验证“文件查找体验是否符合用户直觉”。

建议从“文件名搜索”和“代码内容搜索”两个维度验证。

### 7.1 准备测试样本

建议在测试分支或测试目录下新增以下样本：

1. 空文件：

```text
.env.local
```

2. 配置文件：

```text
config/board-search.config.json
```

3. 文档文件：

```text
docs/search/pin-mapping-notes.md
```

4. 代码文件：

```text
src/app/tools/file-search/file-search-session.service.ts
src/app/tools/file-search/file-search-popup.component.ts
```

5. 被 ignore 的测试文件：

```text
tmp-ignored/search-sample.txt
```

并在 `.gitignore` 中临时加入：

```text
tmp-ignored/
```

6. 在代码文件中加入几个明确可搜索的标识符，例如：

```ts
createFileSearchSession
updateFileSearchQuery
rankFileSearchHits
```

### 7.2 验证文件搜索体验

验证以下查询是否符合预期：

1. 精确文件名片段搜索：
   - `**/*file-search-session*`
   - 预期：`src/app/tools/file-search/file-search-session.service.ts` 排在前面

2. 路径片段搜索：
   - `**/*pin*map*`
   - 预期：`docs/search/pin-mapping-notes.md` 能进入结果

3. 配置文件搜索：
   - `**/*board*config*`
   - 预期：`config/board-search.config.json` 能进入结果

4. 空文件搜索：
   - `**/*env*`
   - 预期：`.env.local` 能被找到

5. 多层路径搜索：
   - `src/**/file-search*`
   - 预期：`src/app/tools/file-search/file-search-session.service.ts` 和相关文件进入结果

6. ignore 验证：
   - 默认搜索 `**/*search-sample*`
   - 预期：`tmp-ignored/search-sample.txt` 不出现
   - 打开 `includeIgnoredFiles`
   - 预期：该文件可以出现

7. 放宽 pattern 验证：
   - 先搜 `src/**/board-config*`
   - 如果无结果，再搜 `**/*board*config*`
   - 预期：放宽后能稳定命中 `config/board-search.config.json`

### 7.3 验证代码内容搜索体验

验证以下内容查询：

1. 搜索精确标识符：
   - `createFileSearchSession`
   - 预期：返回对应代码文件和行号

2. 搜索普通文本：
   - `rankFileSearchHits`
   - 预期：能定位到定义位置

3. 搜索跨文件公共词：
   - `file search`
   - 预期：结果数量可控，前几项集中在新建测试文件，不应被无关大文件淹没

4. 搜索被 ignore 内容：
   - `search-sample`
   - 预期：默认不返回 `tmp-ignored/search-sample.txt`
   - 开启 `includeIgnoredFiles` 时才返回

5. 先 glob 再 grep 验证：
   - 先 `glob_search("**/*file-search*")`
   - 再 `grep_search("createFileSearchSession")`
   - 预期：`grep_search` 能集中命中新建测试文件，而不是在全仓分散返回

### 7.4 验证体验指标

除了“有没有结果”，建议重点观察以下体验指标：

- 首结果是否符合直觉。
- 空文件能否被找到。
- 配置文件、文档文件能否被找到。
- ignore 行为是否稳定。
- 相同 glob 重复执行时结果顺序是否稳定。
- 工具行为是否和 glob 语义一致。
- 模型是否更容易通过“先 glob、再 grep”打到目标文件。

### 7.5 回归验证

改造后建议至少回归以下真实场景：

1. 在当前仓库中搜索 `**/*lex*agent*bootstrap*`
2. 搜索 `**/*ripgrep*`
3. 搜索 `**/*tool*invocation*formatter*`
4. 搜索 `**/*tool*definitions*`
5. 搜索 `**/*history*`

这些场景能覆盖：

- 精确文件名
- 多层路径匹配
- 配置文件
- 文档文件
- 代码内容

## 8. 结论

当前 `aily-blockly` 的文件查找问题，主要不是工具数量不够，而是两件基础工作没有做好：

- `glob_search` 的底层实现不对
- `glob_search` / `grep_search` 的提示词引导不够具体

也就是说，它并不是直接按路径做 glob，而是先通过内容搜索枚举一批文件，再在 JavaScript 里二次过滤；同时模型也缺少足够明确的“如何先缩小范围再命中内容”的指导。

如果只做一件事，最值得做的是：

> 用 Node.js 自带的 `glob` 替换 `listAllContentFiles()`，并补强 `glob_search` / `grep_search` 的提示词引导，让现有 `glob/grep` 体系能打出更高命中。

这是一个足够轻的优化方向，改动范围可控，但能直接改善：

- 空文件搜不到
- 配置文件搜不到
- 结果不稳定
- 工具行为与名称不一致
- 首轮搜索命中率不高

## 9. 参考实现：Codex 与 Claude 的做法

这次不建议直接照搬这些方案，但它们仍然可以作为实现和提示词设计的参考。

### 9.1 Codex

- `codex-rs/file-search/README.md`
  - <https://github.com/openai/codex/blob/main/codex-rs/file-search/README.md>
- `codex-rs/file-search/src/lib.rs`
  - <https://github.com/openai/codex/blob/main/codex-rs/file-search/src/lib.rs>
- `codex-rs/app-server/src/fuzzy_file_search.rs`
  - <https://github.com/openai/codex/blob/main/codex-rs/app-server/src/fuzzy_file_search.rs>

Codex 的核心思路是：

1. 先遍历真实文件系统，而不是从内容搜索结果反推文件列表
2. 用统一的 ignore 规则控制候选集
3. 用 fuzzy matcher 给结果打分
4. 在 UI 层做会话化、增量化搜索

这些思路说明了一点：

- 文件查找体验真正好的前提，是“文件候选集必须来自真实文件系统”

### 9.2 Claude / Claude Code 风格实现

当前本地 `aily-lex` 已经有一套明显偏 Claude Code 风格的 `glob/grep` 设计，可以直接作为提示词和工具职责拆分的参考：

- 搜索总入口：
  - [../aily-lex/src/tools/common/search.ts](/Users/xiewendao/Documents/aily/aily-lex/src/tools/common/search.ts:7)
- `glob_search` 提示词：
  - [../aily-lex/src/tools/glob/prompt.ts](/Users/xiewendao/Documents/aily/aily-lex/src/tools/glob/prompt.ts:10)
- `grep_search` 提示词：
  - [../aily-lex/src/tools/grep/prompt.ts](/Users/xiewendao/Documents/aily/aily-lex/src/tools/grep/prompt.ts:10)

这套思路的重点是：

1. `glob_search` 用来按路径模式收窄文件范围
2. `grep_search` 用来在较小范围内按内容命中
3. 工具提示词会引导模型减少全仓大范围搜索

这和本报告建议的方向是一致的：保留 `glob/grep` 体系，但把底层实现和提示词引导都做正确。
